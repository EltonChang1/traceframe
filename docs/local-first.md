# Local-first

TraceFrame stores project evidence in `.traceframe/` inside your local project.

Assistant planning is local-first too. The default planner is deterministic and makes no network calls. Optional local LLM integration only runs a command supplied by the user.
