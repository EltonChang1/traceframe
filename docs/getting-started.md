# Getting Started

Run `traceframe init`, then use the Python API to track local data work.

For a complete local check, run an example analysis and then:

```bash
traceframe status
traceframe doctor
traceframe report
```
