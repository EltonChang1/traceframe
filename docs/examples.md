# Examples

Example analyses live in `examples/`.

The ecommerce example demonstrates dataset tracking, filter tracking, SQL, chart evidence, claims, report generation, source-row samples, and chart drilldown metadata.
