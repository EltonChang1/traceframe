# Examples

Example analyses live in `examples/`.

The ecommerce example demonstrates dataset tracking, filter tracking, data quality checks, SQL, chart evidence, claims, report generation, source-row samples, and chart drilldown metadata.

The Polars example demonstrates the same local evidence workflow with Polars input and DuckDB SQL.
